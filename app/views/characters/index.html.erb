<p style="color: green"><%= notice %></p>

<h1>Personajes</h1>

<div id="characters">
  <% @characters.each do |character| %>
    <%= render character %>
    <p>
      <%= link_to "Ver Personaje", character %>
    </p>
  <% end %>
</div>

<%= link_to "New character", new_character_path %>
