<p style="color: green"><%= notice %></p>

<h1>Statuses</h1>

<div id="statuses">
  <% @users.each do |user| %>
    <%= render user %>
    <p>
      <%= link_to "Show this status", user %>
    </p>
  <% end %>
</div>

<%= link_to "Nuevo usuario", new_user_path %>
